apiVersion: builtin
kind: HelmChartInflationGenerator
metadata:
  name: psmdb-operator
valuesInline:
  watchAllNamespaces: true
  replicaCount: 1
  resources:
    requests:
      cpu: "0"
      memory: 128Mi
    limits:
      memory: 128Mi
